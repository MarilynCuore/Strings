
//Poner en mayusculas 
let  amigo = 'amigo'
let mayuscula = amigo.charAt(0).toUpperCase()
let restOfWord =  amigo.slice(1)
let allWord = mayuscula + restOfWord
console.log(allWord)

//Poner en minusculas 
let amigo = 'Amigo'
amigo(lowerCase(amigo))
function lowerCase(amigo){
    return amigo.charAt(0).toUpperCase() + amigo.slice[1]
}